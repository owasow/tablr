% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inline-helpers.R
\name{pv}
\alias{pv}
\title{Format P-value for LaTeX (Inline)}
\usage{
pv(x, var = NULL, digits = 3)
}
\arguments{
\item{x}{A numeric p-value, tibble/data.frame cell, OR a fitted model object}

\item{var}{Character string naming the coefficient (required if x is a model)}

\item{digits}{Number of decimal places (default: 3). Only used for exact
p-values above 0.05.}
}
\value{
LaTeX-formatted p-value string (e.g., "$p$ < 0.01" or "$p$ = 0.072")
}
\description{
Formats a p-value for LaTeX inline reporting. Accepts either a raw numeric
p-value or a model object with variable name. Returns a LaTeX math-mode
string with categorical thresholds.
}
\details{
This is the LaTeX counterpart to \code{\link{p}()}, which returns plain text.
Designed for use in \code{\\Sexpr\{\}} in .Rnw files.
}
\examples{
# With raw numeric
pv(0.03)
# Returns: "$p$ < 0.05"

pv(0.002)
# Returns: "$p$ < 0.01"

# With model object
m <- lm(mpg ~ wt, data = mtcars)
pv(m, "wt")

# In .Rnw file: \Sexpr{pv(0.03)} or \Sexpr{pv(model, "treat")}
}
