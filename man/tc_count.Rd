% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/texcount.R
\name{tc_count}
\alias{tc_count}
\title{Count Words in a TeX Document}
\usage{
tc_count(file = NULL, include_bib = TRUE, include_headers = FALSE)
}
\arguments{
\item{file}{Path to .tex file. If NULL, attempts to find .tex file matching
current knitr input file.}

\item{include_bib}{Logical. Include bibliography in count? Default TRUE.}

\item{include_headers}{Logical. Include header words in total? Default FALSE.}
}
\value{
A list with components:
\item{text}{Words in text (body)}
\item{headers}{Words in headers}
\item{outside}{Words outside text (footnotes, captions)}
\item{total}{Total word count (text + outside, optionally + headers)}
\item{raw}{Raw texcount output}
}
\description{
Runs texcount on a .tex file and returns parsed word counts.
For R Markdown documents, this counts the .tex file from a previous render.
}
\examples{
\dontrun{
# In an R Markdown document:
counts <- tc_count()
counts$total

# Specify file directly:
counts <- tc_count("my_paper.tex")
}
}
